import { expect, fixture, html } from '@open-wc/testing';
//TODO: Test has been commented out while we figure out how to setup import maps for the test environment
// import { UmbUserInputElement } from './picker-user.element';
// import { defaultA11yConfig } from '@umbraco-cms/internal/test-utils';

// describe('UmbUserInputElement', () => {
// 	let element: UmbUserInputElement;
// 	beforeEach(async () => {
// 		element = await fixture(html`<umb-user-input></umb-user-input>`);
// 	});

// 	it('is defined with its own instance', () => {
// 		expect(element).to.be.instanceOf(UmbUserInputElement);
// 	});

// 	it('passes the a11y audit', async () => {
// 		await expect(element).shadowDom.to.be.accessible(defaultA11yConfig);
// 	});
// });
